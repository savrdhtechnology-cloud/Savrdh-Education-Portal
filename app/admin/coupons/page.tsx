import { AdminResourceManager } from '@/components/admin-resource-manager';import { ADMIN_RESOURCES } from '@/lib/admin-resources';export default function Page(){const config=ADMIN_RESOURCES['coupons'];return <AdminResourceManager resource="coupons" config={config}/>}
