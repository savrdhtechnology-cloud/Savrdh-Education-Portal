import { createServiceClient,isSupabaseConfigured } from '@/lib/supabase/server';
export default async function Page(){let k={students:0,courses:0,enrollments:0,revenue:0,certificates:0,live:0};if(isSupabaseConfigured()){try{const s=createServiceClient();const [students,courses,enrollments,payments,certs,live]=await Promise.all([s.from('profiles').select('*',{count:'exact',head:true}).eq('role','student'),s.from('courses').select('*',{count:'exact',head:true}),s.from('enrollments').select('*',{count:'exact',head:true}),s.from('payments').select('amount_paise').eq('status','captured'),s.from('certificates').select('*',{count:'exact',head:true}).eq('revoked',false),s.from('live_classes').select('*',{count:'exact',head:true}).gte('starts_at',new Date().toISOString())]);k={students:students.count||0,courses:courses.count||0,enrollments:enrollments.count||0,revenue:(payments.data||[]).reduce((a:any,p:any)=>a+p.amount_paise,0),certificates:certs.count||0,live:live.count||0}}catch{}}return <><div className="pageHead"><div><h1>Admin Dashboard</h1><p>Operations, learning content, payments and platform analytics.</p></div></div><div className="kpiGrid"><div className="kpi"><small>Total Students</small><strong>{k.students}</strong></div><div className="kpi"><small>Total Courses</small><strong>{k.courses}</strong></div><div className="kpi"><small>Total Enrollments</small><strong>{k.enrollments}</strong></div><div className="kpi"><small>Captured Revenue</small><strong>₹{(k.revenue/100).toLocaleString('en-IN')}</strong></div><div className="kpi"><small>Certificates Issued</small><strong>{k.certificates}</strong></div><div className="kpi"><small>Upcoming Live Classes</small><strong>{k.live}</strong></div></div><div className="panel"><h2>Production checklist</h2><p>Configure dedicated Supabase and Razorpay environment variables, apply migrations, set the first super admin role and deploy to Vercel.</p></div></>}
