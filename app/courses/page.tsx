import Link from 'next/link';import { SiteHeader } from '@/components/site-header';import { SiteFooter } from '@/components/site-footer';import { CourseCard } from '@/components/course-card';import { getCourses } from '@/lib/data';
export const metadata={title:'All Courses'}
export default async function Page(){const courses=await getCourses();return <><SiteHeader/><section className="pageHero"><div className="container"><h1>All Courses</h1><p>Choose an English, Maths, combo or live batch. Prices and course access are managed individually.</p></div></section><section className="section"><div className="container"><div className="filters"><Link className="pill" href="/courses">All</Link><Link className="pill" href="/courses/english">English</Link><Link className="pill" href="/courses/maths">Maths</Link></div><div className="courseGrid">{courses.map(c=><CourseCard key={c.id} course={c}/>)}</div></div></section><SiteFooter/></>}
