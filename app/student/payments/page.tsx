import { createClient,isSupabaseConfigured } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];if(isSupabaseConfigured()){const s=await createClient();const {data}=await s.from('payments').select('id,amount_paise,currency,status,gateway_payment_id,created_at,courses(title)').order('created_at',{ascending:false});rows=data||[]}return <><div className="pageHead"><div><h1>Payment History</h1><p>Verified transaction history for your account.</p></div></div><div className="tableWrap"><table><thead><tr><th>Course</th><th>Amount</th><th>Status</th><th>Payment ID</th><th>Date</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.courses?.title}</td><td>₹{(r.amount_paise/100).toLocaleString('en-IN')}</td><td>{r.status}</td><td>{r.gateway_payment_id||'—'}</td><td>{new Date(r.created_at).toLocaleString('en-IN')}</td></tr>)}</tbody></table></div></>}
