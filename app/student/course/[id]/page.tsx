import { CoursePlayer } from '@/components/course-player';import { createClient,isSupabaseConfigured } from '@/lib/supabase/server';import { notFound } from 'next/navigation';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;let title='Savrdh Education Course';if(isSupabaseConfigured()){const s=await createClient();const {data:en}=await s.from('enrollments').select('courses(title)').eq('course_id',id).eq('status','active').maybeSingle();if(!en)notFound();title=(en as any).courses?.title||title}return <><div className="pageHead"><div><h1>{title}</h1><p>Lessons unlock according to your enrollment and course structure.</p></div></div><CoursePlayer courseId={id} title={title}/></>}
