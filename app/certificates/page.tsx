'use client'
import { useState } from 'react';import { useRouter } from 'next/navigation';import { SiteHeader } from '@/components/site-header';import { SiteFooter } from '@/components/site-footer';
export default function Page(){const [id,setId]=useState('');const router=useRouter();return <><SiteHeader/><section className="pageHero"><div className="container"><h1>Certificate Verification</h1><p>Verify a Savrdh Education certificate using its unique Certificate ID.</p></div></section><section className="section"><div className="container" style={{maxWidth:650}}><form className="authCard" onSubmit={e=>{e.preventDefault();if(id.trim())router.push(`/certificates/verify/${encodeURIComponent(id.trim())}`)}}><label>Certificate ID<input value={id} onChange={e=>setId(e.target.value)} placeholder="SAV-EDU-..." required/></label><button className="btn btnGold">Verify Certificate</button></form></div></section><SiteFooter/></>}
