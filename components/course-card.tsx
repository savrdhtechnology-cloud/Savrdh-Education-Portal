import Link from 'next/link'
import { BookOpen, Radio, Award } from 'lucide-react'
import { inr, type DemoCourse } from '@/lib/demo-data'
export function CourseCard({course}:{course:DemoCourse}){const price=course.discount_price_paise??course.price_paise;return <article className={`courseCard accent-${course.accent}`}><div className="courseThumb"><span>{course.category.toUpperCase()}</span><strong>{course.title}</strong></div><div className="courseBody"><h3>{course.title}</h3><p>{course.short_description}</p><div className="priceRow"><strong>{inr(price)}</strong>{course.discount_price_paise&&<del>{inr(course.price_paise)}</del>}</div><Link className="btn btnGold full" href={`/course/${course.slug}`}>View Course</Link><div className="courseMeta"><span><BookOpen size={14}/>{course.lesson_count}+ Lessons</span>{course.live_class_enabled&&<span><Radio size={14}/>Weekly Live</span>}{course.certificate_enabled&&<span><Award size={14}/>Certificate</span>}</div></div></article>}
